# use --global flag if you want to set it for whole machine
git config user.name "codego"
git config user.email "codego7250@gmail.com"

# unset with
# git config --global --unset-all user.name
# git config --global --unset-all user.email
